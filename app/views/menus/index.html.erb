<h1><%= t('views.index.list', brand_name: @restaurant.brand_name) %></h1>

<p><%= link_to t('views.show.create_menu'), new_restaurant_menu_path(@restaurant) %></p>

<% @menus.each do |menu| %>
  <%= link_to menu.name, menu_path(menu) %><br>
<% end %>
