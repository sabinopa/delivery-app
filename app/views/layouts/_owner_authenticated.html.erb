<div class="container d-flex justify-content-between">
  <ul class="navbar-nav">
    <% if @restaurant&.persisted? %>
      <li class="nav-item">
        <%= link_to 'Meu Restaurante', restaurant_path(@restaurant), class: "btn btn-primary nav-link" %>
      </li>
    <% end %>
  </ul>

  <ul class="navbar-nav">
    <li class="nav-item">
      <span class="nav-link active" aria-current="page"><%= current_owner.description %></span>
    </li>
    <li class="nav-item">
      <%= button_to t('helpers.submit.log_out'), destroy_owner_session_path, method: :delete, class: "btn btn-danger nav-link" %>
    </li>
  </ul>
</div>

